# Monte dans /opt/spark/conf/spark-defaults.conf (spark-master, voir
# docker-compose.yaml) : evite de repeter --packages a chaque spark-submit
# manuel. Cote Airflow, SparkSubmitOperator tourne dans le conteneur Airflow
# (pas spark-master), donc ce fichier ne le couvre pas : voir
# airflow_project/dags/spark_helpers.py pour l'equivalent cote DAG.
spark.jars.packages org.postgresql:postgresql:42.7.4
