# Dependances python additionnelles utilisees par les DAGs (providers, libs metier, ...)
# L'image apache/airflow embarque deja Airflow lui-meme, ne pas le remettre ici.
#
# apache-airflow-providers-fab : Airflow 3 utilise par defaut le "Simple Auth Manager"
# (identifiants generes aleatoirement, non adapte a une formation). On installe FAB pour
# retrouver la gestion classique utilisateur/mot de passe (airflow users create).
apache-airflow-providers-fab
# 5.3.1 (et 5.4.0/5.4.1/5.5.x) importent `WEB_COLORS` depuis airflow.settings,
# retire par Airflow 3.3.1 (ImportError au chargement des DAGs). Verifie sur
# le code source : 5.6.0 est la derniere version 5.x sans cet import (6.0.0
# restructure la dependance pyspark en profondeur, evite ici).
apache-airflow-providers-apache-spark==5.6.0
pyspark==4.1.3
kaggle==1.6.17
python-dotenv==1.0.1
snowflake-connector-python==3.12.3
